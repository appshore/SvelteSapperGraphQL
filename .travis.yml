sudo: false
language: node_js
node_js:
  - 'stable'
env:
  global:
    - BUILD_TIMEOUT=10000
install:
  - npm install
  - npm install cypress
services:
  - mongodb
# before_script:
#   - sleep 15
#   - mongo sapeur --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'
